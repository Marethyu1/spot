import {ADD_DOGS} from "../consts/dogs";

const dogs = (state=[], action) => {
    switch (action.type) {
        case ADD_DOGS:
            return state
        default:
            return state
    }
}

export default dogs
